# Copy this file to .env and update with your actual values
# Database Configuration
DB_URL=jdbc:postgresql://localhost:5432/personal_finance
DB_USERNAME=postgres
DB_PASSWORD=your_secure_password_here

# Server Configuration
SERVER_PORT=8080

# JWT Configuration
# IMPORTANT: Generate a secure random secret key (minimum 256 bits / 32 characters)
# You can generate one using: openssl rand -base64 32
JWT_SECRET=your-secret-key-change-this-in-production-minimum-256-bits-required-for-hmac-sha-256-algorithm
JWT_EXPIRATION=86400000
JWT_ISSUER=personal-finance-server

# Spring Profile
# Options: dev, prod
SPRING_PROFILES_ACTIVE=dev

# Timezone
TIMEZONE=Asia/Kolkata

# Budget Configuration
BUDGET_COACH_THRESHOLD=10

# Rate Limiting Configuration
RATE_LIMIT_AUTH=5
RATE_LIMIT_API=100
RATE_LIMIT_BURST=10

# CORS Configuration (comma-separated)
CORS_ALLOWED_ORIGINS=http://localhost:5173,http://localhost:3000,http://localhost:8080

