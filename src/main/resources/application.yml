spring:
  profiles:
    active: ${SPRING_PROFILES_ACTIVE:dev}
  application:
    name: personal-finance-server

# Default configuration - profile-specific configs override these
# Use application-dev.yml for development
# Use application-prod.yml for production
